package patterns.strategy.behaviors;

/**
 * The QuackBehavior can be utilized & reused by any class as these are not limited or hidden under a specific type
 */
public interface QuackBehavior {

    void quack();

}
